import {
  Button,
  ScrollView,
  Text
} from "react-native";
import React from 'react';
import styles from "./style";

const Menu = () => {
  return (
    <ScrollView>
      <Text style={styles.center}>Teste de menu</Text>
    </ScrollView>
  );
}

export default Menu